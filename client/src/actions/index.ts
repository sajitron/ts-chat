import * as axios from 'axios';
import { AUTH_ERROR, AUTH_USER } from './types';

// register user action
export const register = (formProps: any, callback: any) => async (dispatch: any) => {
	console.log(formProps);
};
